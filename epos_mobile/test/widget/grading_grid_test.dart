// test/widget/grading_grid_test.dart
//
// Fichier committé VIDE (0 octet) dans eb172b2. Un fichier de test sans `main`
// casse la compilation de TOUTE la suite `flutter test` (« Undefined name
// 'main' »), pas seulement de ce fichier — les autres tests ne tournent alors
// jamais. On pose donc un `main` minimal en attendant la vraie couverture de
// GradingGrid (sous-critères : cf. catalogue S3-1/S3-2).

import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder volontairement vide : voir l'en-tête. À remplacer par les
  // scénarios S3-1 (rendu hiérarchique parent/enfant) et S3-3 (pondération
  // BINAIRE vs NUMERIQUE) quand la grille à sous-critères sera exercée.
  test('placeholder — GradingGrid non encore couvert (S3-1/S3-2)', () {
    expect(true, isTrue);
  });
}
