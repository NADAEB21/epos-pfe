spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/scoring_db
    username: admin
    password: pfe_password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

eureka:
  client:
    enabled: false

logging:
  level:
    tn.epos.scoring_service: INFO